# AGENTS.md

## 项目简介

- [项目定位，一句话说明项目是什么]
- [项目类型，如：这是一个 npm 项目，根目录有 package.json]

## 范围

- 本仓库默认语言: [编程语言]
- 允许修改目录: [目录列表]
- 禁止修改目录: [目录列表]

## 改动检查

**改动后必须执行:**

- [命令1]
- [命令2]

**检查项:**

- [检查项1]
- [检查项2]

## 交付格式

- [交付格式要求, 比如: 先给风险摘要，再给修改点，再给测试结果]
- 所有文件引用都要带路径和行号
- 对于技能变更，说明变更后对用户的影响

## 项目结构

[项目结构说明，最多4级，重点说明各目录作用]

- 指出项目的目录结构，不要完整列出所有目录和文件，最多4级。
- 重点说明各个文件和目录的作用，尤其是封装公共方法、公共样式、公共请求的文件和目录。
- 对于后端代码，需要指出哪些目录是用于定义接口路由、定义控制器、定义service、定义数据库model的。
- 如果某个目录已经足够表明含义，就不需要继续列出其下的文件、子目录。比如 `src/images/` 目录先显而易见里面都是图片文件或者按目录形式组织的图片文件，那就没有必要继续在项目结构里列出 `src/images/` 下的内容了。

## AI 能力模型

### 感知能力

- **模糊地带主动澄清**：执行任何操作前主动识别未明确表述的前提假设和可选决策路径，将隐性因素转化为明确的可见规则，拒绝在信息不完整的状态下贸然行动。
- **用户意图精准识别**：从"先给方案，不要直接改"等约束性表述中准确捕捉用户对执行流程的深层要求，而非仅理解表面需求。
- **冗余模式自动识别**：在复杂的技能文档中快速定位重复内容（如多处出现的对比表格、重叠的验证步骤），并判断是否可合并或外移。
- **规范差异即时感知**：对照技能编写规范，实时识别当前实现与标准要求的偏差点（如 description 混入实现细节、步骤结构不符合最佳实践）。
- **流程依赖感知**：识别任务步骤之间的前后依赖关系，发现先执行的检查可能被后续改动失效等隐性流程问题。
- **上下文约束识别**：从用户命令、项目规范和技能说明中识别当前应采用的执行模式、语言风格、验证要求和提交规则。
- **差异来源定位**：区分用户目标改动、验证过程中发现的附带不一致，以及工具自动格式化产生的变化，避免混淆变更来源。

### 学习能力

- **多源交叉矛盾定位**：针对同一主题的不同信息来源进行系统化对比分析，不将差异视为独立偶发问题，而是归类为体系层面的结构性冲突加以处理。
- **偏离原点回退机制**：发现执行路径偏离初始定义时，直接回归到原始基准状态重新开始，而非在已发生偏移的基础上进行渐进式修补。
- **规范提取与即时应用**：从 skill-guide.md 等参考文档中快速提取核心编写规范，并直接应用于当前技能的优化过程中。
- **优化模式提炼复用**：从历史技能优化经验中提炼"参考内容外移""步骤合并""精简 description"等可复用模式，快速应用于新的技能优化场景。
- **结构调整反馈学习**：根据 lint 检查和一致性验证的反馈，即时调整优化策略，确保最终输出符合所有约束条件。
- **规则即时迁移**：将项目规范中的抽象约束转化为当前任务的具体执行顺序和操作边界，避免机械照搬规则文本。
- **反馈驱动修正**：根据验证结果、工具输出和用户反馈识别实际偏差，并把权威来源中的正确内容同步到派生位置。
- **技能流程吸收**：读取外部技能或流程说明后，将其步骤、决策分支和输出格式转化为当前会话中的实际执行流程。

### 推理能力

- **结果导向逆向验证**：从期望达成的最终状态或标准行为模式出发，反向推导现有规则的合理性与完备性，而非仅沿着规则设定的方向进行顺向确认。
- **改动影响因果分析**：在执行每一步修改前，预判该改动对相关文件（如 README.md、examples、resources）的连锁影响，主动扫描引用同一模板、章节或示例的关联位置，确保一致性同步。
- **质量标准逻辑验证**：对照质量三重检验标准，逐条验证每条能力描述的通用性、可执行性和指导价值，确保输出质量。
- **结果有效性逆推**：从最终交付物必须满足的验证条件反推检查顺序、执行边界和必要补救步骤，确保验证结果在任务结束时仍然有效。
- **连锁影响分析**：判断一个文件或流程调整是否会触发其他一致性检查、模板同步或派生文档更新，提前识别必须联动的范围。
- **提交意图归纳**：从 diff、用户目标和验证过程归纳本次改动的真实目的，形成能解释为什么修改的提交信息或变更说明。

### 决策能力

- **必要改动筛选机制**：以"是否实质影响执行质量"作为唯一判断标尺，主动抑制不必要的修改冲动，严格限定在非改不可的范围内采取行动。
- **可验证目标闭环执行**：将任务目标转化为可量化验证的检查点，按照"设定目标-执行操作-验证结果"的循环模式持续推进，直至所有验证条件全部满足。
- **执行流程阶段化决策**：用户要求"先给方案"时，采用方案-确认-执行的三阶段流程；无明确要求时，直接执行，保持交互效率。
- **最小必要改动选择**：限定修改范围，只处理目标变更和验证中发现的必要一致性问题，不主动扩展到无关重构。
- **确认后执行控制**：在计划模式、提交、删除、推送等高影响场景中，先展示方案或操作内容，等待用户确认后再执行。
- **验证闭环执行**：完成修改后按项目规则执行一致性检查、同步检查和 lint，并在验证通过后再进入交付或提交阶段。

### 其他能力

- **命名层级一致性管控**：保证命名方式的精确性与实际覆盖范围相匹配，确保同一逻辑在不同表现形式（文档、代码、配置）中保持统一的层级结构。
- **多文件一致性同步**：确保 SKILL.md、README.md、examples、resources 中对同一事项的描述完全一致，包括 description、步骤编号、术语使用等；当辅助内容与权威来源高度重复时，优先保留单一权威来源并删除冗余副本。
- **目录结构分层设计**：根据内容性质（执行步骤/参考文档/示例）合理分配到不同目录层级，主文件保持精简，辅助内容集中管理。
- **输出格式标准化执行**：严格遵循技能定义的输出格式（如验证报告结构、提交信息格式），确保输出结果可预期、可解析。
- **权威来源同步能力**：识别权威来源与派生副本之间的关系，用权威来源修复 README、模板或技能说明中的不一致内容。
- **提交原子性维护**：将同一逻辑目标下的流程修正和必要一致性修复归入同一提交，同时避免混入无关文件。
- **规范化中文交付能力**：使用简体中文、结构化格式和精简说明输出修改原因、影响范围、验证结果和提交信息。

## 路径格式规范

- 在文档中提及文件路径时，优先使用相对路径，以保持跨设备下的通用性
- 在终端中提及文件路径时，优先使用绝对路径，以方便终端/IDE 将其识别为可点击的链接
- 使用正斜杠作为路径分隔符，路径包含空格时使用引号包裹，以确保跨平台兼容性和正确解析

## 需要遵守的规则

- [规则文件引用或内联规则]

## 关键参考

- [关键参考文件列表]
- `.editorconfig` - 编辑器配置，编写内容时需遵循
- `README.md` - 项目说明文档

---
name: yy-refresh
description: >
  刷新 AI 对代码的理解。当用户提到"刷新"、"重新理解"、"刷新缓存"、"重新加载"等内容时使用此技能。此技能会对比所有改动的文件，重新读取并理解人类可能修改过的代码，确保 AI 的上下文与最新代码状态同步。
---

# 代码刷新助手

> 帮助 AI 重新理解最新改动的代码，确保上下文与最新状态同步。

## 使用场景

当用户提到以下内容时，使用此 skill：

- 刷新代码理解
- 重新理解改动
- 刷新缓存
- 重新加载代码
- 对比文件变化
- 同步最新代码

## 工作流程

### 阶段一：获取改动文件列表

1. 获取所有改动的文件（包括已暂存和未暂存）：

   ```bash
   git diff --name-only HEAD
   git diff --cached --name-only
   git status --short
   ```

2. 合并所有改动文件，去除重复

### 阶段二：对比文件变化

对于每个改动的文件，读取并对比：

1. **已暂存的文件** → 对比 `git diff --cached [file]`
2. **未暂存的文件** → 对比 `git diff [file]`
3. **新文件** → 读取完整内容

### 阶段三：重新理解代码

对于改动较大的关键文件，重新分析：

1. 读取文件的完整内容（对于小文件）或关键部分（对于大文件）
2. 识别改动涉及的：
   - 函数/方法变化
   - 接口/类型变化
   - 逻辑变化
   - 依赖变化
3. 总结改动要点

### 阶段四：输出刷新结果

```markdown
## 刷新结果

已刷新理解以下文件：

### [filename1]
- 改动类型: [新增/修改/删除]
- 改动内容: [简要描述]
- 关键变化: [函数/接口/逻辑变化]

### [filename2]
- ...

## 总结

共刷新了 N 个文件的理解，当前 AI 上下文已与最新代码状态同步。
```

## 工具使用

1. **git**: 获取改动文件列表和差异
2. **read**: 读取文件内容
3. **grep**: 搜索关键变化

## 开始对话

当用户启动此 skill 时，请按以下方式响应：

```markdown
我来帮你刷新对代码的理解。

首先获取所有改动的文件，对比变化后重新读取关键代码...
```

然后按照工作流程逐步执行刷新。

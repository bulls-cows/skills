[project]
name = "yy-read-pdf-scripts"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pymupdf"]

[project.optional-dependencies]
dev = ["ruff>=0.11"]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

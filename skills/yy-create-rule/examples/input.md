# 输入示例

本文档展示用户如何请求创建规则文档。

## 示例 1：创建最佳实践规则

```text
记录一条规则：Vue 组件中不要直接修改 props，要通过 emit 事件通知父组件修改。
```

## 示例 2：记录 bug 修复经验

```text
记录一个 bug 修复经验：iOS WebView 中 position:fixed 在软键盘弹起时会失效。
```

## 示例 3：记录架构决策

```text
记录一个架构决策：所有 API 错误都要用统一的错误处理器，不能自己 catch 后静默忽略。
```

## 示例 4：更新现有规则

```text
在 vue组件规范.md 中补充一条关于生命周期钩子的规范。
```

## 示例 5：指定目录

```text
在 .agents/rules/frontend 目录下创建一个关于 CSS 命名的规范文档。
```

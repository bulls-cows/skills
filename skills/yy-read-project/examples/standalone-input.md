# 输入示例：独立项目局部阅读

本文档展示 `yy-read-project` 在独立项目局部阅读场景下的典型输入。

## 示例 1：只看某个模块

```text
请用 yy-read-project 看一下这个项目的 `src/auth` 模块。
我不需要全项目展开，只想知道它在整体里的位置、主要职责，以及它和用户、权限、接口层是怎么接起来的。
```

## 示例 2：只看某个主题

```text
先别整体介绍，帮我只看这个独立项目的 i18n 方案。
我想知道语言包放哪、由谁切换语言、有没有默认语言和回退策略。
```

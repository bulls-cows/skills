# Vue3 TypeScript 规范

本规范定义 TypeScript 在 Vue3 项目中的类型使用约定。

---

## 一、类型注解要求

- **参数**：函数参数必须明确类型
- **返回值**：函数返回值必须明确类型
- **变量**：变量声明必须明确类型

## 二、禁止使用 `any`

**禁止**使用 `any` 类型，应使用以下替代：

- `unknown`：用于类型不确定的场景
- 具体类型/接口：定义准确的数据结构

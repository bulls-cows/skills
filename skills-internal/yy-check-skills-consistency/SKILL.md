---
name: yy-check-skills-consistency
description: >
  检查并修复 README.md 中的技能列表一致性。用于校验 skills/ 目录与技能表格的排序、条目和说明；
  允许 README 保留更简练但准确的归纳，不用于逐字同步 SKILL.md 的 description 原文。
---

# yy-check-skills-consistency

## 描述

检查 README.md 中的技能列表与实际 skills/ 目录内容的一致性，验证排序、条目和说明是否匹配，并修复发现的问题。

说明同步时，`SKILL.md` 的 frontmatter `description` 是技能用途的语义权威来源，但不是 README 说明列的逐字覆盖模板。README 中已有的归纳表达只要足够简练且语义准确，就应保留。

## 使用场景

- 需要验证 README.md 中的技能列表是否按字母顺序排序
- 需要验证技能列表与 skills/ 目录实际内容一致
- 需要修复技能列表的不一致问题
- 需要同步 README.md 技能列表说明，但希望保持简练表达

不应触发：

- 只是查看 README.md 内容
- 只是检查 skills/ 目录内容
- 只是想把 `SKILL.md` 的 `description` 原文整段复制到 README.md
- 不是技能相关的文档一致性检查

## 指令

### 步骤 1. 读取 README.md

- 读取项目根目录的 README.md 文件
- 提取技能列表部分的内容

### 步骤 2. 读取 skills/ 目录

- 列出 skills/ 目录下的所有子目录
- 过滤出有效的技能目录（包含 SKILL.md 的目录）
- 读取每个 SKILL.md 的 frontmatter `description` 字段，作为技能用途的语义权威来源
- 不要把 `description` 默认视为 README 说明列的逐字覆盖模板

### 步骤 3. 验证字母顺序排序

- 检查 README.md 中的技能列表是否按字母顺序排序
- 如果未排序，记录需要调整的顺序

### 步骤 4. 验证条目一致性

- 比较 README.md 中的技能列表与 skills/ 目录中的实际技能
- 识别出只在 README.md 中存在的技能（冗余项）
- 识别出只在 skills/ 目录中存在的技能（缺失项）

### 步骤 5. 验证描述一致性与简练度

- 将 README.md 表格"说明"列中的描述与 SKILL.md 的 frontmatter `description` 逐条做语义比对
- 判断 README 现有说明是否已满足"简练且准确"的要求
- 识别出需要保留的技能说明（可保留项）
- 识别出语义偏离的技能说明（语义不一致项）
- 识别出语义基本正确但明显冗长的技能说明（需简化项）

**判定规则：**

- **简练且准确**：通常只保留核心动作、关键触发条件或关键边界，不展开长串示例，不堆叠实现细节，且不偏离技能真实用途
- **明显冗长**：直接复用大量触发示例、边界清单或实现细节，导致 README 技能列表难以快速浏览
- **语义偏离**：遗漏核心用途、错误描述适用范围，或把非关键细节误写成主用途

### 步骤 6. 生成验证报告

- 输出排序检查结果
- 输出条目一致性检查结果
- 输出描述一致性检查结果
- 区分列出可保留项、语义不一致项和需简化项

### 步骤 7. 修复问题

**决策分支**：

- **发现排序问题**：按字母顺序重新排列技能列表
- **发现缺失项**：在 README.md 中添加相应的技能条目，并基于 `description` 归纳出简练说明
- **发现冗余项**：从 README.md 中删除对应的技能条目
- **发现语义不一致项**：以 `description` 为语义权威来源，重写 README.md 中对应条目的说明，并收敛为简练表达
- **发现需简化项**：保留原有归纳方向，只压缩冗长部分，不替换为新的同义表述
- **说明已简练且准确**：保留 README.md 中原有说明，不做替换
- **无问题**：跳过修复

### 步骤 8. 输出结果

```markdown
## 验证结果

### 排序检查

- 状态: ✅ 通过 / ❌ 未通过
- 详情: [具体说明]

### 条目一致性检查

- 状态: ✅ 通过 / ❌ 未通过
- 缺失项: [技能列表]
- 冗余项: [技能列表]

### 描述一致性检查

- 状态: ✅ 通过 / ❌ 未通过
- 可保留项: [技能名称及原因]
- 语义不一致项: [技能名称及差异说明]
- 需简化项: [技能名称及压缩原因]

### 修复操作

- [已执行 / 未执行] 修复操作
- 详情: [具体修复内容]
```
